# Сводка агента D (ci-supply-chain) — 2026-09-24, HEAD 7726b46

Полный отчёт: `../ci-supply-chain-audit.md` (848 строк: §0–10, таблица требований, наброски `rust-toolchain.toml`/`ci.yml` v2/`release.yml` v2/`dependabot.yml`/`scorecard.yml`, целевые `deny.toml`/`.cargo/audit.toml`). Ниже — финальный ответ агента как есть.

**summary:** CI на `main` красный с 10.04 и красный сегодня (run 35933020292): Security Audit — 18 уязвимостей (к 17 из фазы 1 добавился `rustls 0.23.37`, RUSTSEC-2026-0285 от 14.09), Clippy — 2 ошибки от stable 1.98 (в июле на 1.97 падали три другие строки) — структурная причина: `@stable` без `rust-toolchain.toml` при `pedantic` + `-D warnings`. Из 18: 6 закрываются `cargo update` уже сейчас, 4 — iroh 1.1 (M18), 8 — quick-xml ×4 версии в egui/rfd/notify-rust/iroh-стеке (bump egui 0.36 или временный ignore со сроком). Supply chain workflow'ов не соответствует ни одному пункту §6.6.5: 0 SHA-пинов, нет `permissions` в ci/android, `contents: write` всем jobs release.yml при persisted-токене, и в этом же job скачивается и исполняется непроверенный код (`linuxdeploy` `continuous` + plugin из `master`, без checksum); нет `--locked`, релиз с rust-cache, нет Dependabot и schedule-audit. Релизные assets защищены только `.sha256`-сайдкарами того же job — нет `SHA256SUMS`, подписи, attestations, SBOM, cargo-auditable, проверки тег↔версия; релиз всегда «latest» (сломает канал `0.5.0-beta.N` и сайт); `if-no-files-found: ignore` + `|| true` скрывают потерю DMG/AppImage; AppImage с ubuntu-24.04 (glibc 2.39) не запустится на «Ubuntu 22.04+» из INSTALL.md. Android: NDK 26.1 (r28c `28.2.13676358` предустановлен на раннере), `cargo-ndk` без версии, Gradle-обёртки нет (только `.properties`) → системный Gradle 9.7.1 при AGP 8.7.0, `android.yml` не бежал с 10.04 из-за `paths`, а `release` зависит от android — тихая поломка заблокирует desktop-релиз. deny/audit: 9 из 22 ignore-ID указывают на крейты, которых нет в lock, 10 — транзитивные unmaintained (одна настройка `unmaintained = "workspace"`), `publish = false` нигде, `[graph]` нет (fips/DPI-фичи вне лицензионной проверки); GHSA-h37v-hp6w-2pp8 (ml-dsa `use_hint`) в RustSec отсутствует — видит только Dependabot. Новый блокер: нет `LICENSE-MIT`/`LICENSE-APACHE` и README при «MIT OR Apache-2.0» и двух публичных релизах — нужно до тега v0.4.0 и до заявки в SignPath.

**blockers:**
- B1 CI-гейты красные — `ci.yml:37,53-62`; jobs `107423723908` (18 vulns), `107423723873` (clippy 1.98: `crates/aira-net/src/discovery.rs:155`, `relay.rs:112`) → M18.
- B2 APK не подписан (известно) — `release.yml:225-236`, `mobile/android/app/build.gradle.kts:23-32`; в CI нет keystore/`apksigner` → M19b.
- B3 Нет LICENSE-файлов и README — `Cargo.toml:18` `license = "MIT OR Apache-2.0"`, корень без `LICENSE*`/`README*` → M18, до v0.4.0.
- B4 Непроверенный код в релизном job с write-токеном — `release.yml:11-12` + `:127-131` (linuxdeploy `continuous`, plugin `raw/master`), `checkout@v4` без `persist-credentials: false` → M18.

**top_findings:**
- HIGH | Все actions без SHA-пинов, старые мажоры на Node 20 | `ci.yml:17-58`, `android.yml`, `release.yml` | SHA-пины (таблица §6.1, снята `git ls-remote` 24.09) + `dependabot.yml` | M18
- HIGH | Нет `permissions`/least privilege, `contents: write` везде в release | `release.yml:11-12`; ci/android — блока нет | `permissions: {}` + per-job, `persist-credentials: false` | M18
- HIGH | Нет `--locked`, релиз с rust-cache | `ci.yml:37,51`; `release.yml:42,56,165,179-199` | `--locked` везде, кэш убрать из релиза | M18 / M23
- HIGH | Assets без подписи/attestations/SBOM/auditable/SHA256SUMS | `release.yml:59-153,259-270` | `SHA256SUMS` + `attest-build-provenance@v4.2.2` (`subject-checksums`, permissions `id-token/attestations/artifact-metadata: write`; это SLSA L2, L3 = reusable workflow) + `cargo auditable` + `cargo cyclonedx` + `attest-sbom`, minisign опц. | M23
- HIGH | Релиз без проверок тег↔версии/main/CI; `release.sh` пушит мимо PR; тег v0.3.5 переставлялся | `release.sh:58-59`, `main.wxs:35`, `release.yml:3-5` | job `verify`, `environment: release`, ruleset `v*` | M18 / M23
- HIGH | Android-CI: NDK 26.1, cargo-ndk без версии, нет `gradlew` (Gradle 9.7.1 системный), `paths`-фильтр, release зависит от android | `android.yml:6-18,44,128`, `release.yml:9,170,227,248` | NDK `28.2.13676358`, `cargo-ndk@4.1.2`, wrapper 8.11.1 + `setup-gradle@v6`, `android-check` в ci.yml, decoupling | M19b (android-check — M18)
- MEDIUM | deny/audit: 9 мёртвых ignore, 10 лишних, `publish=false` нет, `[graph]` нет, `yanked` warn | `deny.toml:24-72`, `.cargo/audit.toml:7-38` | целевые файлы §4.5 | M18 / M19b
- MEDIUM | audit-инструменты компилируются каждый прогон (~7,5 мин), версии плавают, нет schedule | `ci.yml:59-62` | cargo-deny-action@v2.1.1, install-action cargo-audit@0.22.2, `--deny yanked`, cron | M18
- MEDIUM | Только ubuntu; нет MSRV, wasm32, concurrency, timeout | `ci.yml` | матрица ubuntu/windows/macos, `msrv` 1.91, `wasm32` non-blocking | M18 / M23
- MEDIUM | AppImage/tar.gz с glibc 2.39 vs INSTALL «22.04+» | `release.yml:22`, `INSTALL.md:104-105` | `container: ubuntu:22.04`, проверка `GLIBC_` | M23 (желательно к v0.4.0)
- MEDIUM | Плавающие раннеры (`macos-latest` = macOS 26), `LSMinimumSystemVersion 10.13` фикция | `release.yml:22-33`, `Info.plist.template:27` | пин `ubuntu-24.04`/`macos-15`/`windows-2025`, `MACOSX_DEPLOYMENT_TARGET=11.0` | M23
- MEDIUM | Тихая потеря артефактов; релиз всегда latest/non-draft | `release.yml:114-153,260-262` | `if-no-files-found: error`, `test -f`, `draft`/`prerelease`/`make_latest` | M23
- MEDIUM | Нет Dependabot; GHSA-only дефекты ml-dsa невидимы для cargo audit | `.github/` | `dependabot.yml` + alerts | M18
- LOW | `dev`/`milestone/M10-*` в триггерах не существуют; `rust-version` только у aira-gui; versionCode без pre-release; `${{ github.ref_name }}` в pwsh; `*.jks` не в .gitignore; архивы без LICENSE | разные | см. §1.3, §2.6–2.7 | M18–M23

**plan_changes (spec/18-milestones.md):** §16.1 — решение 9 (CI-инфра в M23 или отдельный трек «M22-infra»); M18 п.8 заменить на полный список CI-гвардов (toolchain-пин, `--locked`, SHA-пины+Dependabot, permissions/persist-credentials, schedule, msrv/wasm32/android-check, `cargo update` для 6 записей, решение по quick-xml, чистка deny/audit + `publish=false`, linuxdeploy по тегу + вендоринг plugin, job `verify`, `if-no-files-found: error`, починка clippy 1.98); M18 п.9 — LICENSE-MIT/APACHE + README до тега v0.4.0, APK из assets v0.4.0 убрать; M19b п.10–11 — конкретика §2.6/§3 (environment `android-release`, 4 секрета, `apksigner verify --print-certs`, отпечаток в docs, NDK `28.2.13676358`, `cargo-ndk@4.1.2`, Gradle wrapper, `android.yml` без `paths`, decoupling, versionCode); M21 п.1 — `aira-relay` в матрице релиза + контейнер ghcr с provenance/SBOM; M23 п.1 — ОС-матрица блокирующая + coverage; M23 п.4 — переписать: SLSA L2 (не L3), `SHA256SUMS`+attest, auditable, cyclonedx+attest-sbom, контейнер ubuntu:22.04, пин раннеров, без кэша, `SOURCE_DATE_EPOCH`/remap, `environment: release`, draft/prerelease, minisign, scorecard/harden-runner, INSTALL.md (`gh attestation verify`, Sequoia, glibc); M23 п.3 — CONTRIBUTING/CODEOWNERS/шаблоны; «для 1.0» — reusable workflow (L3), harden-runner block, signed tags. Полные формулировки — §9.2 отчёта.

**open_questions:** (1) настройки репо — дефолтные права `GITHUB_TOKEN`, allowed actions, fork-approval, rulesets `main`/`v*`, Dependabot alerts, secret scanning, private vulnerability reporting; (2) бюджет/заявки — Apple $99, SignPath (после LICENSE+README), Android developer verification $25 + ID — ключ подписи нужен до регистрации; кто хранит keystore и minisign-ключ (офлайн, бэкап ×2); (3) APK в v0.4.0/бете — убрать до подписи (рекомендация) или публиковать unsigned; (4) quick-xml — временный ignore со сроком или блокировать M18 на bump egui 0.36; (5) Windows/macOS-раннеры на каждый PR или только main/теги; (6) Linux baseline — контейнер ubuntu:22.04 или объявить glibc ≥ 2.39; (7) нумерация — M23 по спеке или отдельный «M22-infra»; (8) SLSA L2 для беты, L3 к 1.0; (9) `environment: release` с ручным approve на каждый релиз; (10) ветка `dev` — завести или убрать из правил/CI.

**Не проверено** (§10 отчёта): локальные `cargo deny/audit/build`, состав релизных бинарей (lock ≠ бинарь), AGP 8.7 ↔ Gradle 9.7.1, версии glibc (по памяти), SLSA-уровни (docs.github.com заблокирован), граф iroh 1.1, CVE-2026-24850, лицензия aws-lc-sys, настройки GitHub, cargo-ndk и 16 KB, `Duration::from_hours` в MSRV 1.91, `cargo wix` с pre-release.
