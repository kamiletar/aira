# Key Derivation Contexts

**Канонический список — `docs/KEY_CONTEXTS.md`** (корень репозитория). Этот файл раньше дублировал его и устарел;
не редактировать здесь — только там. Правило: один контекст → один ключ → одна цель; новый контекст добавляется
в таблицу до написания кода.

Исключение (не KDF от seed): хоп-идентичность и KEM-ключи хопа для Aira Onion (M24b) берутся из локального RNG —
чтобы контакт не мог найти IP пользователя в записях хопов по чат-ключу.
